# Release v1.2.4

Version alignment, copy-integrity, and docs that match what the package ships.

## What changed

- Host manifests and `package-lock.json` report `1.2.4` with `package.json`.
- `npm test` runs `scripts/check-rule-copies.js` (AGENTS copies, Cursor skills, Copilot invariants).
- README / AGENTS.md: ecosystem agents are not in this npm tarball.
- `/lexis help` is documented as command files, not `skills/lexis-two-help/`.
- `file:.` self-link documented: do not recurse `node_modules/@draig/lexis-two`.

## Install / upgrade

```bash
npm install -D @draig/lexis-two@1.2.4
```

## Docs

- [Changelog](../../CHANGELOG.md)

## Verify

```bash
npm test
```
