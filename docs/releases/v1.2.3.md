# Release v1.2.3

Version alignment across package manifests.

## What changed

- Bumped version to `1.2.3` in `package.json`, `package-lock.json`, `opencode.json`, `gemini-extension.json`, `pi-extension/package.json`, and other host manifests so installed copies report the same version.

## Install / upgrade

```bash
npm install -D @draig/lexis-two@1.2.3
```

## Docs

- [Changelog](CHANGELOG.md)

## Verify

```bash
npm test
node scripts/check-rule-copies.js
```
