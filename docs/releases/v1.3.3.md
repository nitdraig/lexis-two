# Release v1.3.3

Discovery, public `/lexis` surface, Command Code notes, and `/desx` design audit.

## What changed

- `/discx` (Discovery), `/specx` (Specxis), `/lexis` (intensity + quality verbs).
- Design audit: `/desx audit` / `/desx apply`, `scripts/desx-audit.js`, `DESIGN-AUDIT.md`.
- Host manifests and `package-lock.json` report `1.3.3` with `package.json`.

## Install / upgrade

```bash
npm install -D @draig/lexis-two@1.3.3
```

## Docs

- [Changelog](../../CHANGELOG.md)
- [DESX.md](../../DESX.md)

## Verify

```bash
npm test
```
