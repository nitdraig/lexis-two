---
description: "Run design-auditor. Writes DESIGN-AUDIT.md only."
---

Run design audit for: $ARGUMENTS

Follow `skills/desx/SKILL.md`. Role: `design-auditor`. Run the Node detector (no API key). Optionally polish descriptions. Write only `DESIGN-AUDIT.md`. Never edit components or styles. Never tick apply-checkboxes.

Chat: Spanish. File: keep the audit template language.
