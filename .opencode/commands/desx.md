---
description: "Design audit. Default /desx audit. Apply with /desx apply."
---

Handle the design request: $ARGUMENTS

If empty or `audit`: follow `skills/desx/SKILL.md` (`design-auditor`). Run `scripts/desx-audit.js` (or `node_modules/@draig/lexis-two/scripts/desx-audit.js`). Write only `DESIGN-AUDIT.md`. No UI edits. No build/test.

If `apply`: follow `skills/desx-apply/SKILL.md`. You are the implementer, not `design-auditor`. Read the audit file, apply P0→P1→P2/P3, tick only what you fixed, re-run the detector.

Chat: Spanish. Docs: English.
